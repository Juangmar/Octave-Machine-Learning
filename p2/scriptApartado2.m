%drawData2data2 = data = load('ex2data2.txt', '-ascii');y = data(:,3);x = data(:,1:2);X = [ones(rows(y),1), x];%2.1map = mapFeature(data2(:,1),data2(:,2));%2.2theta = zeros(size(map, 2), 1);la = 1;[cos, th] = costeR(theta, map, data(:,3), la);% cos = 0.69315%2.3opciones = optimset('GradObj', 'on', 'MaxIter', 400);[final_theta, cos] = fminunc(@(t)(costeR(t, map, y, la)), theta, opciones);plotDecisionBoundary(final_theta, map, y);