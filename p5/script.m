clear;warning('off', 'Octave:possible-matlab-short-circuit-operator');%===============================================================================%===============================================================================%==================== APARTADO 1: REG.LINEAL REGU.  ============================%===============================================================================%===============================================================================load('ex5data1.mat');wholambda = 1;theta_in = [1;1];m = size(X,1);x = [ones(m, 1) X];[J, theta] = linearRegReg(x, y, theta_in, lambda);th_optimizado = optimizar(x, y, lambda);l = x*th_optimizadohold on;plot(X,l, 'LineWidth', 1);plot(X, y, 'x', 'MarkerSize',10);hold of;